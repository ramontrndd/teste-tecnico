import { CurrecyMastkDirective } from './currecy-mastk.directive';

describe('CurrecyMastkDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrecyMastkDirective();
    expect(directive).toBeTruthy();
  });
});
